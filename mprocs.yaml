procs:
  client:
    cwd: packages/client
    shell: pnpm run dev
  contracts:
    cwd: packages/contracts
    shell: pnpm run dev
  anvil:
    cwd: packages/contracts
    shell: anvil --base-fee 0 --block-time 1
