// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { FieldLayout } from "@latticexyz/store/src/FieldLayout.sol";
import { Schema } from "@latticexyz/store/src/Schema.sol";
import { EncodedLengths, EncodedLengthsLib } from "@latticexyz/store/src/EncodedLengths.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";

// Import user types
import { EncounterType } from "./../common.sol";

struct CombatEncounterData {
  EncounterType encounterType;
  uint256 start;
  uint256 end;
  uint256 currentTurn;
  uint256 maxTurns;
  bytes32[] defenders;
  bytes32[] attackers;
}

library CombatEncounter {
  // Hex below is the result of `WorldResourceIdLib.encode({ namespace: "UD", name: "CombatEncounter", typeId: RESOURCE_TABLE });`
  ResourceId constant _tableId = ResourceId.wrap(0x74625544000000000000000000000000436f6d626174456e636f756e74657200);

  FieldLayout constant _fieldLayout =
    FieldLayout.wrap(0x0081050201202020200000000000000000000000000000000000000000000000);

  // Hex-encoded key schema of (bytes32)
  Schema constant _keySchema = Schema.wrap(0x002001005f000000000000000000000000000000000000000000000000000000);
  // Hex-encoded value schema of (uint8, uint256, uint256, uint256, uint256, bytes32[], bytes32[])
  Schema constant _valueSchema = Schema.wrap(0x00810502001f1f1f1fc1c1000000000000000000000000000000000000000000);

  /**
   * @notice Get the table's key field names.
   * @return keyNames An array of strings with the names of key fields.
   */
  function getKeyNames() internal pure returns (string[] memory keyNames) {
    keyNames = new string[](1);
    keyNames[0] = "encounterId";
  }

  /**
   * @notice Get the table's value field names.
   * @return fieldNames An array of strings with the names of value fields.
   */
  function getFieldNames() internal pure returns (string[] memory fieldNames) {
    fieldNames = new string[](7);
    fieldNames[0] = "encounterType";
    fieldNames[1] = "start";
    fieldNames[2] = "end";
    fieldNames[3] = "currentTurn";
    fieldNames[4] = "maxTurns";
    fieldNames[5] = "defenders";
    fieldNames[6] = "attackers";
  }

  /**
   * @notice Register the table with its config.
   */
  function register() internal {
    StoreSwitch.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Register the table with its config.
   */
  function _register() internal {
    StoreCore.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Get encounterType.
   */
  function getEncounterType(bytes32 encounterId) internal view returns (EncounterType encounterType) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return EncounterType(uint8(bytes1(_blob)));
  }

  /**
   * @notice Get encounterType.
   */
  function _getEncounterType(bytes32 encounterId) internal view returns (EncounterType encounterType) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return EncounterType(uint8(bytes1(_blob)));
  }

  /**
   * @notice Set encounterType.
   */
  function setEncounterType(bytes32 encounterId, EncounterType encounterType) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked(uint8(encounterType)), _fieldLayout);
  }

  /**
   * @notice Set encounterType.
   */
  function _setEncounterType(bytes32 encounterId, EncounterType encounterType) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked(uint8(encounterType)), _fieldLayout);
  }

  /**
   * @notice Get start.
   */
  function getStart(bytes32 encounterId) internal view returns (uint256 start) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Get start.
   */
  function _getStart(bytes32 encounterId) internal view returns (uint256 start) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 1, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Set start.
   */
  function setStart(bytes32 encounterId, uint256 start) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreSwitch.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((start)), _fieldLayout);
  }

  /**
   * @notice Set start.
   */
  function _setStart(bytes32 encounterId, uint256 start) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreCore.setStaticField(_tableId, _keyTuple, 1, abi.encodePacked((start)), _fieldLayout);
  }

  /**
   * @notice Get end.
   */
  function getEnd(bytes32 encounterId) internal view returns (uint256 end) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 2, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Get end.
   */
  function _getEnd(bytes32 encounterId) internal view returns (uint256 end) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 2, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Set end.
   */
  function setEnd(bytes32 encounterId, uint256 end) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreSwitch.setStaticField(_tableId, _keyTuple, 2, abi.encodePacked((end)), _fieldLayout);
  }

  /**
   * @notice Set end.
   */
  function _setEnd(bytes32 encounterId, uint256 end) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreCore.setStaticField(_tableId, _keyTuple, 2, abi.encodePacked((end)), _fieldLayout);
  }

  /**
   * @notice Get currentTurn.
   */
  function getCurrentTurn(bytes32 encounterId) internal view returns (uint256 currentTurn) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 3, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Get currentTurn.
   */
  function _getCurrentTurn(bytes32 encounterId) internal view returns (uint256 currentTurn) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 3, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Set currentTurn.
   */
  function setCurrentTurn(bytes32 encounterId, uint256 currentTurn) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreSwitch.setStaticField(_tableId, _keyTuple, 3, abi.encodePacked((currentTurn)), _fieldLayout);
  }

  /**
   * @notice Set currentTurn.
   */
  function _setCurrentTurn(bytes32 encounterId, uint256 currentTurn) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreCore.setStaticField(_tableId, _keyTuple, 3, abi.encodePacked((currentTurn)), _fieldLayout);
  }

  /**
   * @notice Get maxTurns.
   */
  function getMaxTurns(bytes32 encounterId) internal view returns (uint256 maxTurns) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 4, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Get maxTurns.
   */
  function _getMaxTurns(bytes32 encounterId) internal view returns (uint256 maxTurns) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 4, _fieldLayout);
    return (uint256(bytes32(_blob)));
  }

  /**
   * @notice Set maxTurns.
   */
  function setMaxTurns(bytes32 encounterId, uint256 maxTurns) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreSwitch.setStaticField(_tableId, _keyTuple, 4, abi.encodePacked((maxTurns)), _fieldLayout);
  }

  /**
   * @notice Set maxTurns.
   */
  function _setMaxTurns(bytes32 encounterId, uint256 maxTurns) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreCore.setStaticField(_tableId, _keyTuple, 4, abi.encodePacked((maxTurns)), _fieldLayout);
  }

  /**
   * @notice Get defenders.
   */
  function getDefenders(bytes32 encounterId) internal view returns (bytes32[] memory defenders) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    bytes memory _blob = StoreSwitch.getDynamicField(_tableId, _keyTuple, 0);
    return (SliceLib.getSubslice(_blob, 0, _blob.length).decodeArray_bytes32());
  }

  /**
   * @notice Get defenders.
   */
  function _getDefenders(bytes32 encounterId) internal view returns (bytes32[] memory defenders) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    bytes memory _blob = StoreCore.getDynamicField(_tableId, _keyTuple, 0);
    return (SliceLib.getSubslice(_blob, 0, _blob.length).decodeArray_bytes32());
  }

  /**
   * @notice Set defenders.
   */
  function setDefenders(bytes32 encounterId, bytes32[] memory defenders) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreSwitch.setDynamicField(_tableId, _keyTuple, 0, EncodeArray.encode((defenders)));
  }

  /**
   * @notice Set defenders.
   */
  function _setDefenders(bytes32 encounterId, bytes32[] memory defenders) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreCore.setDynamicField(_tableId, _keyTuple, 0, EncodeArray.encode((defenders)));
  }

  /**
   * @notice Get the length of defenders.
   */
  function lengthDefenders(bytes32 encounterId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    uint256 _byteLength = StoreSwitch.getDynamicFieldLength(_tableId, _keyTuple, 0);
    unchecked {
      return _byteLength / 32;
    }
  }

  /**
   * @notice Get the length of defenders.
   */
  function _lengthDefenders(bytes32 encounterId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    uint256 _byteLength = StoreCore.getDynamicFieldLength(_tableId, _keyTuple, 0);
    unchecked {
      return _byteLength / 32;
    }
  }

  /**
   * @notice Get an item of defenders.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function getItemDefenders(bytes32 encounterId, uint256 _index) internal view returns (bytes32) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    unchecked {
      bytes memory _blob = StoreSwitch.getDynamicFieldSlice(_tableId, _keyTuple, 0, _index * 32, (_index + 1) * 32);
      return (bytes32(_blob));
    }
  }

  /**
   * @notice Get an item of defenders.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function _getItemDefenders(bytes32 encounterId, uint256 _index) internal view returns (bytes32) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    unchecked {
      bytes memory _blob = StoreCore.getDynamicFieldSlice(_tableId, _keyTuple, 0, _index * 32, (_index + 1) * 32);
      return (bytes32(_blob));
    }
  }

  /**
   * @notice Push an element to defenders.
   */
  function pushDefenders(bytes32 encounterId, bytes32 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreSwitch.pushToDynamicField(_tableId, _keyTuple, 0, abi.encodePacked((_element)));
  }

  /**
   * @notice Push an element to defenders.
   */
  function _pushDefenders(bytes32 encounterId, bytes32 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreCore.pushToDynamicField(_tableId, _keyTuple, 0, abi.encodePacked((_element)));
  }

  /**
   * @notice Pop an element from defenders.
   */
  function popDefenders(bytes32 encounterId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreSwitch.popFromDynamicField(_tableId, _keyTuple, 0, 32);
  }

  /**
   * @notice Pop an element from defenders.
   */
  function _popDefenders(bytes32 encounterId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreCore.popFromDynamicField(_tableId, _keyTuple, 0, 32);
  }

  /**
   * @notice Update an element of defenders at `_index`.
   */
  function updateDefenders(bytes32 encounterId, uint256 _index, bytes32 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    unchecked {
      bytes memory _encoded = abi.encodePacked((_element));
      StoreSwitch.spliceDynamicData(_tableId, _keyTuple, 0, uint40(_index * 32), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Update an element of defenders at `_index`.
   */
  function _updateDefenders(bytes32 encounterId, uint256 _index, bytes32 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    unchecked {
      bytes memory _encoded = abi.encodePacked((_element));
      StoreCore.spliceDynamicData(_tableId, _keyTuple, 0, uint40(_index * 32), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Get attackers.
   */
  function getAttackers(bytes32 encounterId) internal view returns (bytes32[] memory attackers) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    bytes memory _blob = StoreSwitch.getDynamicField(_tableId, _keyTuple, 1);
    return (SliceLib.getSubslice(_blob, 0, _blob.length).decodeArray_bytes32());
  }

  /**
   * @notice Get attackers.
   */
  function _getAttackers(bytes32 encounterId) internal view returns (bytes32[] memory attackers) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    bytes memory _blob = StoreCore.getDynamicField(_tableId, _keyTuple, 1);
    return (SliceLib.getSubslice(_blob, 0, _blob.length).decodeArray_bytes32());
  }

  /**
   * @notice Set attackers.
   */
  function setAttackers(bytes32 encounterId, bytes32[] memory attackers) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreSwitch.setDynamicField(_tableId, _keyTuple, 1, EncodeArray.encode((attackers)));
  }

  /**
   * @notice Set attackers.
   */
  function _setAttackers(bytes32 encounterId, bytes32[] memory attackers) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreCore.setDynamicField(_tableId, _keyTuple, 1, EncodeArray.encode((attackers)));
  }

  /**
   * @notice Get the length of attackers.
   */
  function lengthAttackers(bytes32 encounterId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    uint256 _byteLength = StoreSwitch.getDynamicFieldLength(_tableId, _keyTuple, 1);
    unchecked {
      return _byteLength / 32;
    }
  }

  /**
   * @notice Get the length of attackers.
   */
  function _lengthAttackers(bytes32 encounterId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    uint256 _byteLength = StoreCore.getDynamicFieldLength(_tableId, _keyTuple, 1);
    unchecked {
      return _byteLength / 32;
    }
  }

  /**
   * @notice Get an item of attackers.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function getItemAttackers(bytes32 encounterId, uint256 _index) internal view returns (bytes32) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    unchecked {
      bytes memory _blob = StoreSwitch.getDynamicFieldSlice(_tableId, _keyTuple, 1, _index * 32, (_index + 1) * 32);
      return (bytes32(_blob));
    }
  }

  /**
   * @notice Get an item of attackers.
   * @dev Reverts with Store_IndexOutOfBounds if `_index` is out of bounds for the array.
   */
  function _getItemAttackers(bytes32 encounterId, uint256 _index) internal view returns (bytes32) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    unchecked {
      bytes memory _blob = StoreCore.getDynamicFieldSlice(_tableId, _keyTuple, 1, _index * 32, (_index + 1) * 32);
      return (bytes32(_blob));
    }
  }

  /**
   * @notice Push an element to attackers.
   */
  function pushAttackers(bytes32 encounterId, bytes32 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreSwitch.pushToDynamicField(_tableId, _keyTuple, 1, abi.encodePacked((_element)));
  }

  /**
   * @notice Push an element to attackers.
   */
  function _pushAttackers(bytes32 encounterId, bytes32 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreCore.pushToDynamicField(_tableId, _keyTuple, 1, abi.encodePacked((_element)));
  }

  /**
   * @notice Pop an element from attackers.
   */
  function popAttackers(bytes32 encounterId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreSwitch.popFromDynamicField(_tableId, _keyTuple, 1, 32);
  }

  /**
   * @notice Pop an element from attackers.
   */
  function _popAttackers(bytes32 encounterId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreCore.popFromDynamicField(_tableId, _keyTuple, 1, 32);
  }

  /**
   * @notice Update an element of attackers at `_index`.
   */
  function updateAttackers(bytes32 encounterId, uint256 _index, bytes32 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    unchecked {
      bytes memory _encoded = abi.encodePacked((_element));
      StoreSwitch.spliceDynamicData(_tableId, _keyTuple, 1, uint40(_index * 32), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Update an element of attackers at `_index`.
   */
  function _updateAttackers(bytes32 encounterId, uint256 _index, bytes32 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    unchecked {
      bytes memory _encoded = abi.encodePacked((_element));
      StoreCore.spliceDynamicData(_tableId, _keyTuple, 1, uint40(_index * 32), uint40(_encoded.length), _encoded);
    }
  }

  /**
   * @notice Get the full data.
   */
  function get(bytes32 encounterId) internal view returns (CombatEncounterData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreSwitch.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Get the full data.
   */
  function _get(bytes32 encounterId) internal view returns (CombatEncounterData memory _table) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    (bytes memory _staticData, EncodedLengths _encodedLengths, bytes memory _dynamicData) = StoreCore.getRecord(
      _tableId,
      _keyTuple,
      _fieldLayout
    );
    return decode(_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function set(
    bytes32 encounterId,
    EncounterType encounterType,
    uint256 start,
    uint256 end,
    uint256 currentTurn,
    uint256 maxTurns,
    bytes32[] memory defenders,
    bytes32[] memory attackers
  ) internal {
    bytes memory _staticData = encodeStatic(encounterType, start, end, currentTurn, maxTurns);

    EncodedLengths _encodedLengths = encodeLengths(defenders, attackers);
    bytes memory _dynamicData = encodeDynamic(defenders, attackers);

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using individual values.
   */
  function _set(
    bytes32 encounterId,
    EncounterType encounterType,
    uint256 start,
    uint256 end,
    uint256 currentTurn,
    uint256 maxTurns,
    bytes32[] memory defenders,
    bytes32[] memory attackers
  ) internal {
    bytes memory _staticData = encodeStatic(encounterType, start, end, currentTurn, maxTurns);

    EncodedLengths _encodedLengths = encodeLengths(defenders, attackers);
    bytes memory _dynamicData = encodeDynamic(defenders, attackers);

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function set(bytes32 encounterId, CombatEncounterData memory _table) internal {
    bytes memory _staticData = encodeStatic(
      _table.encounterType,
      _table.start,
      _table.end,
      _table.currentTurn,
      _table.maxTurns
    );

    EncodedLengths _encodedLengths = encodeLengths(_table.defenders, _table.attackers);
    bytes memory _dynamicData = encodeDynamic(_table.defenders, _table.attackers);

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreSwitch.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Set the full data using the data struct.
   */
  function _set(bytes32 encounterId, CombatEncounterData memory _table) internal {
    bytes memory _staticData = encodeStatic(
      _table.encounterType,
      _table.start,
      _table.end,
      _table.currentTurn,
      _table.maxTurns
    );

    EncodedLengths _encodedLengths = encodeLengths(_table.defenders, _table.attackers);
    bytes memory _dynamicData = encodeDynamic(_table.defenders, _table.attackers);

    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreCore.setRecord(_tableId, _keyTuple, _staticData, _encodedLengths, _dynamicData, _fieldLayout);
  }

  /**
   * @notice Decode the tightly packed blob of static data using this table's field layout.
   */
  function decodeStatic(
    bytes memory _blob
  )
    internal
    pure
    returns (EncounterType encounterType, uint256 start, uint256 end, uint256 currentTurn, uint256 maxTurns)
  {
    encounterType = EncounterType(uint8(Bytes.getBytes1(_blob, 0)));

    start = (uint256(Bytes.getBytes32(_blob, 1)));

    end = (uint256(Bytes.getBytes32(_blob, 33)));

    currentTurn = (uint256(Bytes.getBytes32(_blob, 65)));

    maxTurns = (uint256(Bytes.getBytes32(_blob, 97)));
  }

  /**
   * @notice Decode the tightly packed blob of dynamic data using the encoded lengths.
   */
  function decodeDynamic(
    EncodedLengths _encodedLengths,
    bytes memory _blob
  ) internal pure returns (bytes32[] memory defenders, bytes32[] memory attackers) {
    uint256 _start;
    uint256 _end;
    unchecked {
      _end = _encodedLengths.atIndex(0);
    }
    defenders = (SliceLib.getSubslice(_blob, _start, _end).decodeArray_bytes32());

    _start = _end;
    unchecked {
      _end += _encodedLengths.atIndex(1);
    }
    attackers = (SliceLib.getSubslice(_blob, _start, _end).decodeArray_bytes32());
  }

  /**
   * @notice Decode the tightly packed blobs using this table's field layout.
   * @param _staticData Tightly packed static fields.
   * @param _encodedLengths Encoded lengths of dynamic fields.
   * @param _dynamicData Tightly packed dynamic fields.
   */
  function decode(
    bytes memory _staticData,
    EncodedLengths _encodedLengths,
    bytes memory _dynamicData
  ) internal pure returns (CombatEncounterData memory _table) {
    (_table.encounterType, _table.start, _table.end, _table.currentTurn, _table.maxTurns) = decodeStatic(_staticData);

    (_table.defenders, _table.attackers) = decodeDynamic(_encodedLengths, _dynamicData);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function deleteRecord(bytes32 encounterId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function _deleteRecord(bytes32 encounterId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    StoreCore.deleteRecord(_tableId, _keyTuple, _fieldLayout);
  }

  /**
   * @notice Tightly pack static (fixed length) data using this table's schema.
   * @return The static data, encoded into a sequence of bytes.
   */
  function encodeStatic(
    EncounterType encounterType,
    uint256 start,
    uint256 end,
    uint256 currentTurn,
    uint256 maxTurns
  ) internal pure returns (bytes memory) {
    return abi.encodePacked(encounterType, start, end, currentTurn, maxTurns);
  }

  /**
   * @notice Tightly pack dynamic data lengths using this table's schema.
   * @return _encodedLengths The lengths of the dynamic fields (packed into a single bytes32 value).
   */
  function encodeLengths(
    bytes32[] memory defenders,
    bytes32[] memory attackers
  ) internal pure returns (EncodedLengths _encodedLengths) {
    // Lengths are effectively checked during copy by 2**40 bytes exceeding gas limits
    unchecked {
      _encodedLengths = EncodedLengthsLib.pack(defenders.length * 32, attackers.length * 32);
    }
  }

  /**
   * @notice Tightly pack dynamic (variable length) data using this table's schema.
   * @return The dynamic data, encoded into a sequence of bytes.
   */
  function encodeDynamic(bytes32[] memory defenders, bytes32[] memory attackers) internal pure returns (bytes memory) {
    return abi.encodePacked(EncodeArray.encode((defenders)), EncodeArray.encode((attackers)));
  }

  /**
   * @notice Encode all of a record's fields.
   * @return The static (fixed length) data, encoded into a sequence of bytes.
   * @return The lengths of the dynamic fields (packed into a single bytes32 value).
   * @return The dynamic (variable length) data, encoded into a sequence of bytes.
   */
  function encode(
    EncounterType encounterType,
    uint256 start,
    uint256 end,
    uint256 currentTurn,
    uint256 maxTurns,
    bytes32[] memory defenders,
    bytes32[] memory attackers
  ) internal pure returns (bytes memory, EncodedLengths, bytes memory) {
    bytes memory _staticData = encodeStatic(encounterType, start, end, currentTurn, maxTurns);

    EncodedLengths _encodedLengths = encodeLengths(defenders, attackers);
    bytes memory _dynamicData = encodeDynamic(defenders, attackers);

    return (_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Encode keys as a bytes32 array using this table's field layout.
   */
  function encodeKeyTuple(bytes32 encounterId) internal pure returns (bytes32[] memory) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = encounterId;

    return _keyTuple;
  }
}
